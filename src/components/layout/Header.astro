---
interface Props {
  currentPath?: string;
}

const { currentPath = '/' } = Astro.props;
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');

function getHref(path: string) {
  if (path === '/') return baseUrl === '' ? '/' : baseUrl + '/';
  return `${baseUrl}${path}`;
}

function isActive(path: string) {
  const normalizedCurrent = currentPath.endsWith('/') ? currentPath : `${currentPath}/`;
  if (path === '/' && (normalizedCurrent === '/' || normalizedCurrent === `${baseUrl}/`)) return true;
  return path !== '/' && normalizedCurrent.includes(path);
}

const navLinks = [
  { name: 'Home', path: '/' },
  { name: 'About Us', path: '/about/' },
  { name: 'Directive', path: '/directive/' },
  { 
    name: 'Activities', 
    path: '/events/', 
    hasDropdown: true,
    dropdownItems: [
      { name: 'Upcoming Events', path: '/events/#upcoming' },
      { name: 'Past Events', path: '/events/#past' },
      { name: 'Academic Calendar', path: '/events/#calendar' }
    ]
  },
  { name: 'Projects', path: '/projects/' },
  { name: 'Opportunities', path: '/opportunities/' },
  { name: 'Gallery', path: '/gallery/' },
  { name: 'News', path: '/news/' },
  { name: 'Contact', path: '/contact/' }
];
---

<header class="site-header" id="main-header">
  <a href="#main-content" class="skip-link">Skip to main content</a>

  <div class="site-container header-inner">
    <!-- Option 2: Geometric White Logo Panel with Diagonal End -->
    <div class="brand-panel-wrapper">
      <a href={getHref('/')} class="brand-panel" aria-label="IEEE Nanotechnology Council Yachay Tech Home">
        <img 
          src={getHref('/assets/logos/ieee_nano_logo.svg')} 
          alt="IEEE Nanotechnology Council Yachay Tech Student Branch Official Logo" 
          width="280" 
          height="54" 
          class="header-logo-img" 
        />
      </a>
      <div class="brand-panel-cyan-accent" aria-hidden="true"></div>
    </div>

    <!-- Desktop Navigation Grid -->
    <nav class="desktop-nav" aria-label="Main Navigation">
      <ul class="nav-list">
        {navLinks.map((link) => (
          <li class={`nav-item ${link.hasDropdown ? 'has-dropdown' : ''}`}>
            {link.hasDropdown ? (
              <div class="dropdown-wrapper">
                <a 
                  href={getHref(link.path)} 
                  class={`nav-link ${isActive(link.path) ? 'active' : ''}`}
                  aria-expanded="false"
                  aria-haspopup="true"
                  id="events-dropdown-btn"
                >
                  {link.name}
                  <svg class="dropdown-icon" viewBox="0 0 24 24" width="14" height="14" aria-hidden="true">
                    <path d="M7 10l5 5 5-5z" fill="currentColor"/>
                  </svg>
                </a>
                <ul class="dropdown-menu" aria-labelledby="events-dropdown-btn" role="menu">
                  {link.dropdownItems?.map((item) => (
                    <li role="none">
                      <a href={getHref(item.path)} class="dropdown-item" role="menuitem">
                        {item.name}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            ) : (
              <a 
                href={getHref(link.path)} 
                class={`nav-link ${isActive(link.path) ? 'active' : ''}`}
                aria-current={isActive(link.path) ? 'page' : undefined}
              >
                {link.name}
              </a>
            )}
          </li>
        ))}
      </ul>
    </nav>

    <!-- Mobile Hamburger Toggle -->
    <button 
      class="mobile-toggle" 
      id="mobile-toggle-btn" 
      aria-label="Toggle navigation menu"
      aria-expanded="false"
      aria-controls="mobile-nav-drawer"
    >
      <span class="hamburger-bar"></span>
      <span class="hamburger-bar"></span>
      <span class="hamburger-bar"></span>
    </button>
  </div>

  <!-- Mobile Navigation Drawer -->
  <div class="mobile-drawer" id="mobile-nav-drawer" aria-hidden="true">
    <div class="mobile-drawer-header">
      <img src={getHref('/assets/logos/ieee_nano_logo.svg')} alt="IEEE NTC Logo" width="240" height="46" />
      <button class="mobile-close-btn" id="mobile-close-btn" aria-label="Close navigation menu">
        &times;
      </button>
    </div>

    <nav class="mobile-nav" aria-label="Mobile Navigation">
      <ul class="mobile-nav-list">
        {navLinks.map((link) => (
          <li class="mobile-nav-item">
            <a href={getHref(link.path)} class={`mobile-nav-link ${isActive(link.path) ? 'active' : ''}`}>
              {link.name}
            </a>
            {link.hasDropdown && (
              <ul class="mobile-subnav-list">
                {link.dropdownItems?.map((item) => (
                  <li>
                    <a href={getHref(item.path)} class="mobile-subnav-link">
                      • {item.name}
                    </a>
                  </li>
                ))}
              </ul>
            )}
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 1000;
    background-color: #071C34;
    border-bottom: 1px solid var(--color-cyan);
    box-shadow: 0 4px 18px rgba(0, 0, 0, 0.35);
    height: 74px;
    display: flex;
    align-items: center;
    width: 100%;
  }

  .skip-link {
    position: absolute;
    top: -100px;
    left: 1rem;
    background: var(--color-cyan);
    color: var(--color-white);
    padding: 0.5rem 1rem;
    font-weight: 700;
    z-index: 2000;
    transition: top 0.2s ease;
    border-radius: 0 0 4px 4px;
  }

  .skip-link:focus {
    top: 0;
  }

  .header-inner {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    gap: clamp(24px, 2.5vw, 56px);
    width: min(calc(100% - 64px), 1680px);
    margin-inline: auto;
  }

  /* Logo Panel Wrapper with Diagonal End and Cyan Accent */
  .brand-panel-wrapper {
    position: relative;
    display: flex;
    align-items: center;
  }

  .brand-panel {
    position: relative;
    width: clamp(280px, 21vw, 320px);
    height: 56px;
    background-color: #ffffff;
    display: flex;
    align-items: center;
    padding: 6px 36px 6px 16px;
    clip-path: polygon(0 0, 94% 0, 86% 100%, 0 100%);
    text-decoration: none;
    z-index: 2;
    transition: opacity 0.2s ease;
  }

  .brand-panel:hover {
    opacity: 0.96;
  }

  .brand-panel-cyan-accent {
    position: absolute;
    top: 0;
    left: 0;
    width: clamp(290px, 21.8vw, 332px);
    height: 56px;
    background-color: var(--color-cyan);
    clip-path: polygon(0 0, 96% 0, 88% 100%, 0 100%);
    z-index: 1;
  }

  .header-logo-img {
    height: 44px;
    width: auto;
    max-width: 100%;
    object-fit: contain;
    display: block;
  }

  /* Desktop Navigation Links */
  .desktop-nav {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    width: 100%;
  }

  .nav-list {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: clamp(14px, 1.5vw, 30px);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .nav-item {
    display: flex;
    align-items: center;
  }

  .nav-link {
    display: inline-flex;
    align-items: center;
    gap: 0.2rem;
    color: rgba(255, 255, 255, 0.9);
    font-family: 'Open Sans', var(--font-body);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    text-decoration: none;
    white-space: nowrap;
    position: relative;
    padding: 0.5rem 0.2rem;
    transition: color 0.2s ease;
  }

  .nav-link:hover,
  .nav-link:focus-visible {
    color: var(--color-cyan);
  }

  .nav-link.active {
    color: var(--color-cyan);
  }

  .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: -6px;
    left: 0;
    right: 0;
    height: 2px;
    background-color: var(--color-cyan);
    border-radius: 1px;
  }

  /* Dropdown Styles */
  .has-dropdown {
    position: relative;
  }

  .dropdown-wrapper {
    position: relative;
  }

  .dropdown-icon {
    transition: transform 0.2s ease;
  }

  .has-dropdown:hover .dropdown-icon,
  .has-dropdown:focus-within .dropdown-icon {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    right: 0;
    min-width: 210px;
    background-color: #071C34;
    border: 1px solid rgba(5, 135, 189, 0.35);
    border-radius: 6px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
    list-style: none;
    padding: 0.5rem 0;
    opacity: 0;
    visibility: hidden;
    transform: translateY(8px);
    transition: opacity 0.2s ease, transform 0.2s ease, visibility 0.2s ease;
    z-index: 1100;
  }

  .has-dropdown:hover .dropdown-menu,
  .has-dropdown:focus-within .dropdown-menu,
  .dropdown-menu.show {
    opacity: 1;
    visibility: visible;
    transform: translateY(6px);
  }

  .dropdown-item {
    display: block;
    padding: 0.6rem 1.2rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.82rem;
    font-weight: 600;
    text-decoration: none;
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .dropdown-item:hover,
  .dropdown-item:focus {
    background-color: var(--color-cyan);
    color: #ffffff;
  }

  /* Mobile Toggle & Drawer */
  .mobile-toggle {
    display: none;
    flex-direction: column;
    justify-content: space-between;
    width: 32px;
    height: 22px;
    background: transparent;
    border: none;
    cursor: pointer;
    padding: 0;
    z-index: 1050;
    justify-self: end;
  }

  .hamburger-bar {
    width: 100%;
    height: 3px;
    background-color: var(--color-white);
    border-radius: 2px;
    transition: all 0.3s ease;
  }

  .mobile-drawer {
    position: fixed;
    top: 0;
    right: 0;
    width: 310px;
    max-width: 85vw;
    height: 100vh;
    background-color: #071C34;
    box-shadow: -8px 0 32px rgba(0, 0, 0, 0.5);
    z-index: 2000;
    display: flex;
    flex-direction: column;
    transform: translateX(100%);
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .mobile-drawer.open {
    transform: translateX(0);
  }

  .mobile-drawer-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1.25rem 1.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .mobile-close-btn {
    background: transparent;
    border: none;
    color: var(--color-white);
    font-size: 2.2rem;
    line-height: 1;
    cursor: pointer;
  }

  .mobile-nav {
    padding: 1.5rem;
    overflow-y: auto;
  }

  .mobile-nav-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    padding: 0;
    margin: 0;
  }

  .mobile-nav-link {
    color: var(--color-white);
    font-size: 1.05rem;
    font-weight: 700;
    text-transform: uppercase;
    text-decoration: none;
    display: block;
    transition: color 0.2s ease;
  }

  .mobile-nav-link.active,
  .mobile-nav-link:hover {
    color: var(--color-cyan);
  }

  .mobile-subnav-list {
    list-style: none;
    padding-left: 1rem;
    margin-top: 0.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .mobile-subnav-link {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.9rem;
    text-decoration: none;
  }

  .mobile-subnav-link:hover {
    color: var(--color-cyan);
  }

  /* Responsive Breakpoint: 1120px to prevent nav item overlap */
  @media (max-width: 1120px) {
    .desktop-nav {
      display: none;
    }
    .mobile-toggle {
      display: flex;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const mobileBtn = document.getElementById('mobile-toggle-btn');
    const mobileDrawer = document.getElementById('mobile-nav-drawer');
    const mobileClose = document.getElementById('mobile-close-btn');

    function openMobileMenu() {
      mobileDrawer?.classList.add('open');
      mobileDrawer?.setAttribute('aria-hidden', 'false');
      mobileBtn?.setAttribute('aria-expanded', 'true');
      document.body.style.overflow = 'hidden';
    }

    function closeMobileMenu() {
      mobileDrawer?.classList.remove('open');
      mobileDrawer?.setAttribute('aria-hidden', 'true');
      mobileBtn?.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    }

    mobileBtn?.addEventListener('click', openMobileMenu);
    mobileClose?.addEventListener('click', closeMobileMenu);

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') {
        closeMobileMenu();
        const dropdownMenu = document.querySelector('.dropdown-menu');
        dropdownMenu?.classList.remove('show');
      }
    });

    const dropdownBtn = document.getElementById('events-dropdown-btn');
    const dropdownWrapper = dropdownBtn?.closest('.has-dropdown');

    dropdownBtn?.addEventListener('focus', () => {
      dropdownBtn.setAttribute('aria-expanded', 'true');
    });

    dropdownWrapper?.addEventListener('focusout', (e) => {
      const relatedTarget = (e as FocusEvent).relatedTarget as HTMLElement;
      if (!dropdownWrapper.contains(relatedTarget)) {
        dropdownBtn?.setAttribute('aria-expanded', 'false');
      }
    });
  });
</script>
