---
import Layout from '@/layouts/Layout.astro';
import { EVENTS_DATA } from '@/data/events';
import { GALLERY_ALBUMS } from '@/data/gallery';

const upcomingEvents = EVENTS_DATA.filter((e) => e.category === 'Próximos');
const pastEvents = EVENTS_DATA.filter((e) => e.category === 'Pasados');

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
function getHref(path: string) {
  if (path === '/') return baseUrl === '' ? '/' : baseUrl + '/';
  return `${baseUrl}${path}`;
}
---

<Layout 
  title="Actividades y Eventos | IEEE Nanotechnology Council Yachay Tech"
  description="Explora los próximos talleres, eventos de vinculación, conferencias científicas y capacitaciones del Capítulo Estudiantil IEEE NTC Yachay Tech."
>
  <!-- Hero Inner Banner -->
  <section class="events-hero">
    <div class="site-container">
      <div class="breadcrumb">
        <a href={getHref('/')}>Inicio</a> &gt; <span>Actividades</span>
      </div>
      <span class="hero-eyebrow">ACTIVIDADES Y COMUNIDAD</span>
      <h1 class="hero-title font-heading">ACTIVIDADES Y EVENTOS</h1>
      <p class="hero-subtitle">
        Explora nuestros próximos webinars, talleres prácticos de laboratorio, conferencias internacionales y jornadas de vinculación comunitaria.
      </p>
    </div>
  </section>

  <!-- Sticky Internal Navigation Bar -->
  <section class="subnav-bar">
    <div class="site-container">
      <div class="subnav-links">
        <a href="#upcoming" class="subnav-btn active">Próximos Eventos ({upcomingEvents.length})</a>
        <a href="#past" class="subnav-btn">Eventos Pasados ({pastEvents.length})</a>
        <a href="#calendar" class="subnav-btn">Calendario Académico</a>
      </div>
    </div>
  </section>

  <!-- Upcoming Events Section -->
  <section class="events-section" id="upcoming">
    <div class="site-container">
      <div class="section-header">
        <span class="section-eyebrow">AGENDA Y WEBINARS</span>
        <h2 class="section-title">PRÓXIMO EVENTO</h2>
      </div>

      <div class="upcoming-single-wrapper">
        {upcomingEvents.map((event) => (
          <article class={`upcoming-featured-card ${!event.image ? 'no-image-card' : ''}`} id={event.slug}>
            {event.image && (
              <div class="upcoming-image-side">
                <img src={getHref(event.image)} alt={event.imageAlt} width="520" height="320" loading="eager" class="featured-img" />
                <div class="upcoming-badges-group">
                  <span class="upcoming-live-badge">
                    <span class="pulse-dot"></span>
                    PRÓXIMO EVENTO
                  </span>
                  <span class="type-pill">{event.type}</span>
                </div>
              </div>
            )}

            <div class="upcoming-content-side">
              {!event.image && (
                <div class="upcoming-badges-top">
                  <span class="upcoming-live-badge">
                    <span class="pulse-dot"></span>
                    PRÓXIMO EVENTO
                  </span>
                  <span class="type-pill">{event.type}</span>
                </div>
              )}
              <div class="meta-bar">
                <span class="meta-date">
                  <svg viewBox="0 0 24 24" width="16" height="16" fill="currentColor" aria-hidden="true"><path d="M19 4h-1V2h-2v2H8V2H6v2H5c-1.11 0-1.99.9-1.99 2L3 20c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 16H5V10h14v10z"/></svg>
                  {event.date} • {event.time}
                </span>
                <span class="modality-tag">{event.modality}</span>
              </div>

              <h3 class="upcoming-title">{event.title}</h3>
              <p class="upcoming-description">{event.description}</p>

              <div class="location-info">
                <svg viewBox="0 0 24 24" width="16" height="16" fill="currentColor" aria-hidden="true"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                <span>{event.location}</span>
              </div>

              <div class="upcoming-action-bar">
                {event.registrationStatus === 'open' && event.registrationUrl ? (
                  <a href={event.registrationUrl} target="_blank" rel="noopener noreferrer" class="btn btn-primary">
                    REGISTRARSE AHORA
                    <svg viewBox="0 0 24 24" width="16" height="16" fill="currentColor" aria-hidden="true"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                  </a>
                ) : (
                  <button class="btn btn-secondary disabled-status-btn" disabled>
                    REGISTRO PRÓXIMAMENTE
                  </button>
                )}
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- Past Events Section -->
  <section class="events-section bg-alt" id="past">
    <div class="site-container">
      <div class="section-header">
        <span class="section-eyebrow">HISTORIAL Y ARCHIVO</span>
        <h2 class="section-title">EVENTOS PASADOS Y VINCULACIÓN</h2>
      </div>

      <div class="past-events-grid">
        {pastEvents.map((event) => {
          const album = GALLERY_ALBUMS.find((a) => a.id === event.galleryAlbumId);
          return (
            <article class="event-grid-card" id={event.slug}>
              <div class="card-image-box">
                <img src={getHref(event.image)} alt={event.imageAlt} width="400" height="225" loading="lazy" class="grid-img" />
                <span class="type-pill past">{event.type}</span>
                <span class="status-completed-badge">COMPLETADO</span>
              </div>

              <div class="card-body">
                <div class="card-meta">
                  <span class="meta-date-text">{event.date}</span>
                  <span class="modality-tag past">{event.modality}</span>
                </div>

                <h3 class="card-grid-title">{event.title}</h3>
                <p class="card-grid-desc">{event.description}</p>

                <div class="grid-location">
                  <svg viewBox="0 0 24 24" width="14" height="14" fill="currentColor" aria-hidden="true"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>
                  <span>{event.location}</span>
                </div>

                <div class="card-actions-row">
                  {event.externalUrl ? (
                    <a href={event.externalUrl} target="_blank" rel="noopener noreferrer" class="btn btn-outline-dark card-btn">
                      VER PROGRAMA
                      <svg viewBox="0 0 24 24" width="14" height="14" fill="currentColor" aria-hidden="true"><path d="M14 3h7v7h-2V6.41l-9.29 9.3-1.42-1.42 9.3-9.29H14V3zM5 5h6v2H5v12h12v-6h2v6c0 1.1-.9 2-2 2H5c-1.1 0-2-.9-2-2V7c0-1.1.9-2 2-2z"/></svg>
                    </a>
                  ) : album ? (
                    <button 
                      class="btn btn-outline-dark card-btn open-event-gallery-btn"
                      data-album={JSON.stringify(album)}
                    >
                      VER FOTOS DE GALERÍA ({album.slides.length})
                    </button>
                  ) : (
                    <a href={getHref('/gallery/')} class="btn btn-outline-dark card-btn">
                      VER FOTOS DE GALERÍA
                    </a>
                  )}
                </div>
              </div>
            </article>
          );
        })}
      </div>
    </div>
  </section>

  <!-- Compact Academic Calendar Block -->
  <section class="events-section" id="calendar">
    <div class="site-container">
      <div class="calendar-compact-card">
        <div class="calendar-info-side">
          <span class="section-eyebrow">CALENDARIO ACADÉMICO</span>
          <h2 class="calendar-title font-heading">CALENDARIO DEL CAPÍTULO 2026</h2>
          <p class="calendar-desc">
            Mantente informado sobre las próximas actividades, talleres técnicos, conferencias internacionales y fechas de convocatorias del capítulo.
          </p>
        </div>

        <div class="calendar-action-side">
          <div class="next-event-preview">
            <span class="preview-label">PRÓXIMO EVENTO CONFIRMADO</span>
            <strong class="preview-title">IEEE NTC Region 9 Student Virtual Summit</strong>
            <span class="preview-date">15 de Octubre de 2026 • Virtual</span>
          </div>

          <a href="#upcoming" class="btn btn-primary calendar-cta">
            VER PRÓXIMO EVENTO
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Interactive Carousel Modal for Event Gallery -->
  <dialog id="event-gallery-modal" class="event-modal-dialog">
    <div class="event-modal-inner">
      <button class="event-modal-close" id="event-modal-close-btn" aria-label="Cerrar galería">&times;</button>
      
      <div class="event-modal-header">
        <div class="modal-header-top">
          <span class="event-modal-category" id="modal-album-category">VINCULACIÓN</span>
          <span class="modal-counter-badge" id="modal-slide-counter">Foto 1 de 4</span>
        </div>
        <h2 class="event-modal-title" id="modal-album-title">Título del Evento</h2>
        <p class="event-modal-summary" id="modal-album-summary">Resumen del evento</p>
      </div>

      <!-- Carousel Viewer Stage with Prominent Lateral Arrows -->
      <div class="event-carousel-stage">
        <button class="carousel-nav-btn prev-btn" id="modal-prev-btn" aria-label="Foto anterior">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="currentColor" aria-hidden="true"><path d="M15.41 7.41L14 6l-6 6 6 6 1.41-1.41L10.83 12z"/></svg>
        </button>
        
        <div class="carousel-slide-box">
          <img id="modal-slide-img" src="" alt="" class="carousel-main-img" />
          <div class="carousel-caption-bar">
            <h4 class="slide-title" id="modal-slide-title">Título de la foto</h4>
            <p class="slide-desc" id="modal-slide-desc">Descripción de la foto</p>
          </div>
        </div>

        <button class="carousel-nav-btn next-btn" id="modal-next-btn" aria-label="Foto siguiente">
          <svg viewBox="0 0 24 24" width="24" height="24" fill="currentColor" aria-hidden="true"><path d="M10 6L8.59 7.41 13.17 12l-4.58 4.59L10 18l6-6z"/></svg>
        </button>
      </div>

      <div class="modal-keyboard-hint">
        <span>Usa las flechas laterales o las teclas &#8592; / &#8594; para navegar</span>
      </div>
    </div>
  </dialog>
</Layout>

<style>
  /* Hero Banner */
  .events-hero {
    background: linear-gradient(135deg, #071C34 0%, var(--color-navy) 60%, #152A47 100%);
    color: var(--color-white);
    padding: 3rem 0 3.5rem;
    border-bottom: 3px solid var(--color-cyan);
  }

  .breadcrumb {
    font-size: 0.85rem;
    color: var(--color-cyan);
    margin-bottom: 0.75rem;
  }

  .breadcrumb a {
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
  }

  .hero-eyebrow {
    display: inline-block;
    font-size: 0.85rem;
    font-weight: 700;
    letter-spacing: 1.5px;
    color: var(--color-cyan);
    margin-bottom: 0.5rem;
  }

  .hero-title {
    font-size: clamp(2rem, 3.5vw, 2.75rem);
    color: var(--color-white);
  }

  .hero-subtitle {
    font-size: 1.05rem;
    color: rgba(255, 255, 255, 0.85);
    margin-top: 0.5rem;
    max-width: 720px;
  }

  /* Subnav Sticky */
  .subnav-bar {
    background-color: var(--color-navy);
    position: sticky;
    top: 74px;
    z-index: 100;
    border-bottom: 1px solid rgba(5, 135, 189, 0.3);
    padding: 0.75rem 0;
  }

  .subnav-links {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
  }

  .subnav-btn {
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    font-size: 0.85rem;
    font-weight: 600;
    padding: 0.4rem 1rem;
    border-radius: 20px;
    transition: all 0.2s ease;
    white-space: nowrap;
  }

  .subnav-btn:hover, .subnav-btn.active {
    background-color: var(--color-cyan);
    color: #ffffff;
  }

  .events-section {
    padding: 4.5rem 0;
  }

  .events-section.bg-alt {
    background-color: var(--color-bg-light);
  }

  /* Featured Single Upcoming Card */
  .upcoming-single-wrapper {
    max-width: 100%;
    margin-top: 1.5rem;
  }

  .upcoming-featured-card {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    background-color: #ffffff;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: var(--shadow-hover);
    border: 1px solid var(--color-border-light);
  }

  .upcoming-featured-card.no-image-card {
    display: block;
    background: linear-gradient(135deg, #071C34 0%, #0c2545 60%, #15325b 100%);
    border: 2px solid #0587bd;
    box-shadow: 0 12px 36px rgba(5, 135, 189, 0.25);
  }

  .upcoming-featured-card.no-image-card .upcoming-content-side {
    padding: 3rem 3.5rem;
    color: #ffffff;
  }

  .upcoming-featured-card.no-image-card .upcoming-title {
    color: #ffffff;
    font-size: 1.85rem;
    margin: 0.75rem 0 1rem;
    line-height: 1.25;
  }

  .upcoming-featured-card.no-image-card .upcoming-description {
    color: rgba(255, 255, 255, 0.88);
    font-size: 1.05rem;
    line-height: 1.6;
    max-width: 960px;
    margin-bottom: 1.5rem;
  }

  .upcoming-featured-card.no-image-card .meta-date {
    color: var(--color-cyan);
    font-size: 0.95rem;
  }

  .upcoming-featured-card.no-image-card .location-info {
    color: rgba(255, 255, 255, 0.85);
    margin-bottom: 2rem;
    font-size: 0.95rem;
  }

  .upcoming-badges-top {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
  }

  .upcoming-image-side {
    position: relative;
    background-color: #071c34;
    min-height: 320px;
  }

  .featured-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .upcoming-badges-group {
    position: absolute;
    top: 1.25rem;
    left: 1.25rem;
    display: flex;
    gap: 0.5rem;
    z-index: 2;
  }

  .upcoming-live-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    background-color: #0587BD;
    color: #ffffff;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.5px;
    padding: 0.35rem 0.8rem;
    border-radius: 20px;
  }

  .pulse-dot {
    width: 8px;
    height: 8px;
    background-color: #ffffff;
    border-radius: 50%;
    animation: pulse 1.5s infinite;
  }

  @keyframes pulse {
    0% { transform: scale(0.95); opacity: 0.8; }
    50% { transform: scale(1.3); opacity: 1; }
    100% { transform: scale(0.95); opacity: 0.8; }
  }

  .type-pill {
    background-color: rgba(7, 28, 52, 0.85);
    color: var(--color-cyan);
    border: 1px solid var(--color-cyan);
    font-size: 0.75rem;
    font-weight: 700;
    padding: 0.35rem 0.8rem;
    border-radius: 20px;
  }

  .type-pill.past {
    position: absolute;
    top: 1rem;
    left: 1rem;
    z-index: 2;
  }

  .status-completed-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background-color: #122844;
    color: #ffffff;
    font-size: 0.7rem;
    font-weight: 700;
    padding: 0.3rem 0.65rem;
    border-radius: 12px;
    z-index: 2;
  }

  .upcoming-content-side {
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .meta-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1rem;
  }

  .meta-date {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--color-cyan);
    font-weight: 700;
    font-size: 0.88rem;
  }

  .modality-tag {
    font-size: 0.75rem;
    background-color: rgba(5, 135, 189, 0.1);
    color: var(--color-navy);
    padding: 0.25rem 0.65rem;
    border-radius: 4px;
    font-weight: 600;
  }

  .upcoming-title {
    font-size: 1.5rem;
    color: var(--color-navy);
    margin-bottom: 0.85rem;
    line-height: 1.3;
  }

  .upcoming-description {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: 1.25rem;
  }

  .location-info {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.85rem;
    color: var(--color-text-muted);
    margin-bottom: 1.75rem;
  }

  /* Past Events Grid */
  .past-events-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 30px;
    margin-top: 2rem;
  }

  .event-grid-card {
    background-color: #ffffff;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: var(--shadow-subtle);
    border: 1px solid var(--color-border-light);
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .event-grid-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-hover);
  }

  .card-image-box {
    position: relative;
    width: 100%;
    height: 220px;
    background-color: #061b31;
  }

  .grid-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .card-body {
    padding: 22px;
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .card-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.75rem;
  }

  .meta-date-text {
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--color-cyan);
  }

  .card-grid-title {
    font-size: 1.15rem;
    color: var(--color-navy);
    margin-bottom: 0.6rem;
    line-height: 1.35;
  }

  .card-grid-desc {
    font-size: 0.88rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    margin-bottom: 1rem;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .grid-location {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.8rem;
    color: var(--color-text-muted);
    margin-bottom: 1.25rem;
    margin-top: auto;
  }

  .card-actions-row {
    padding-top: 1rem;
    border-top: 1px solid rgba(0, 0, 0, 0.06);
  }

  .card-btn {
    width: 100%;
    justify-content: center;
    font-size: 0.82rem;
    font-weight: 700;
    padding: 0.6rem;
  }

  /* Academic Calendar Block */
  .calendar-compact-card {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    background: linear-gradient(135deg, #071c34 0%, var(--color-navy) 100%);
    color: #ffffff;
    border-radius: 12px;
    padding: 2.5rem 3rem;
    gap: 2.5rem;
    align-items: center;
    border: 2px solid var(--color-cyan);
  }

  .calendar-title {
    color: #ffffff;
    font-size: 1.75rem;
    margin-bottom: 0.5rem;
  }

  .calendar-desc {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.95rem;
    line-height: 1.5;
  }

  .calendar-action-side {
    background: rgba(255, 255, 255, 0.05);
    padding: 1.5rem;
    border-radius: 8px;
    border: 1px solid rgba(5, 135, 189, 0.4);
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .preview-label {
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--color-cyan);
    letter-spacing: 0.5px;
  }

  .preview-title {
    font-size: 1rem;
    color: #ffffff;
    display: block;
  }

  .preview-date {
    font-size: 0.8rem;
    color: rgba(255, 255, 255, 0.7);
    display: block;
  }

  /* Event Gallery Lightbox Carousel Modal */
  .event-modal-dialog {
    border: none;
    border-radius: 16px;
    padding: 0;
    max-width: 900px;
    width: 94vw;
    background: #061b31;
    color: #ffffff;
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.6);
    margin: auto;
  }

  .event-modal-dialog::backdrop {
    background: rgba(4, 15, 28, 0.85);
    backdrop-filter: blur(6px);
  }

  .event-modal-inner {
    position: relative;
    padding: 2rem 2.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .event-modal-close {
    position: absolute;
    top: 1rem;
    right: 1.25rem;
    background: transparent;
    border: none;
    font-size: 2.2rem;
    color: #ffffff;
    cursor: pointer;
    line-height: 1;
    transition: color 0.2s ease;
  }

  .event-modal-close:hover {
    color: var(--color-cyan);
  }

  .event-modal-category {
    font-size: 0.75rem;
    font-weight: 700;
    color: var(--color-cyan);
    letter-spacing: 1px;
  }

  .event-modal-title {
    font-size: 1.5rem;
    color: #ffffff;
    margin-top: 0.2rem;
  }

  .event-modal-summary {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.75);
    margin-top: 0.25rem;
  }

  .event-carousel-stage {
    position: relative;
    width: 100%;
    height: 440px;
    background: #020c17;
    border-radius: 10px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .carousel-slide-box {
    position: relative;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .carousel-main-img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    transition: opacity 0.3s ease;
  }

  .carousel-caption-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: linear-gradient(to top, rgba(0, 0, 0, 0.85) 0%, transparent 100%);
    padding: 1.5rem 1.5rem 1rem;
    color: #ffffff;
  }

  .slide-counter {
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--color-cyan);
    text-transform: uppercase;
    display: block;
    margin-bottom: 0.2rem;
  }

  .slide-title {
    font-size: 1.05rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
  }

  .slide-desc {
    font-size: 0.82rem;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.4;
  }

  .carousel-nav-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(5, 135, 189, 0.8);
    color: #ffffff;
    border: none;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    font-size: 1.4rem;
    cursor: pointer;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.2s ease, transform 0.2s ease;
  }

  .carousel-nav-btn:hover {
    background: var(--color-cyan);
    transform: translateY(-50%) scale(1.08);
  }

  .prev-btn { left: 1rem; }
  .next-btn { right: 1rem; }

  .carousel-thumbnails-row {
    display: flex;
    gap: 0.75rem;
    overflow-x: auto;
    padding: 0.5rem 0;
  }

  .carousel-thumb-btn {
    width: 70px;
    height: 48px;
    border-radius: 6px;
    overflow: hidden;
    border: 2px solid transparent;
    background: #020c17;
    cursor: pointer;
    padding: 0;
    flex-shrink: 0;
    opacity: 0.6;
    transition: opacity 0.2s ease, border-color 0.2s ease;
  }

  .carousel-thumb-btn.is-active, .carousel-thumb-btn:hover {
    opacity: 1;
    border-color: var(--color-cyan);
  }

  .carousel-thumb-btn img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 1080px) {
    .past-events-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .upcoming-featured-card {
      grid-template-columns: 1fr;
    }
    .calendar-compact-card {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .past-events-grid {
      grid-template-columns: 1fr;
    }
    .event-carousel-stage {
      height: 280px;
    }
    .event-modal-inner {
      padding: 1.5rem 1rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const baseUrl = '/ieee-ntc-yachay';
    function resolveUrl(path: string) {
      if (!path) return '';
      if (path.startsWith('http') || path.startsWith(baseUrl)) return path;
      return `${baseUrl.replace(/\/$/, '')}/${path.replace(/^\//, '')}`;
    }

    const modal = document.getElementById('event-gallery-modal') as HTMLDialogElement | null;
    const closeBtn = document.getElementById('event-modal-close-btn');
    const prevBtn = document.getElementById('modal-prev-btn');
    const nextBtn = document.getElementById('modal-next-btn');

    const catEl = document.getElementById('modal-album-category');
    const titleEl = document.getElementById('modal-album-title');
    const summaryEl = document.getElementById('modal-album-summary');

    const slideImg = document.getElementById('modal-slide-img') as HTMLImageElement | null;
    const counterEl = document.getElementById('modal-slide-counter');
    const slideTitleEl = document.getElementById('modal-slide-title');
    const slideDescEl = document.getElementById('modal-slide-desc');
    const thumbsRow = document.getElementById('modal-thumbs-row');

    let currentAlbum: any = null;
    let currentIndex = 0;

    function renderSlide(idx: number) {
      if (!currentAlbum || !currentAlbum.slides || currentAlbum.slides.length === 0) return;
      currentIndex = (idx + currentAlbum.slides.length) % currentAlbum.slides.length;
      const slide = currentAlbum.slides[currentIndex];

      if (slideImg) slideImg.src = resolveUrl(slide.image);
      if (counterEl) counterEl.textContent = `Foto ${currentIndex + 1} de ${currentAlbum.slides.length}`;
      if (slideTitleEl) slideTitleEl.textContent = slide.title;
      if (slideDescEl) slideDescEl.textContent = slide.caption;

      document.querySelectorAll('.carousel-thumb-btn').forEach((tb, i) => {
        if (i === currentIndex) tb.classList.add('is-active');
        else tb.classList.remove('is-active');
      });
    }

    document.querySelectorAll('.open-event-gallery-btn').forEach((btn) => {
      btn.addEventListener('click', () => {
        const raw = btn.getAttribute('data-album');
        if (!raw || !modal) return;
        currentAlbum = JSON.parse(raw);
        currentIndex = 0;

        if (catEl) catEl.textContent = currentAlbum.category.toUpperCase();
        if (titleEl) titleEl.textContent = currentAlbum.title;
        if (summaryEl) summaryEl.textContent = currentAlbum.summary;

        if (thumbsRow) {
          thumbsRow.innerHTML = currentAlbum.slides.map((s: any, idx: number) => `
            <button class="carousel-thumb-btn ${idx === 0 ? 'is-active' : ''}" data-index="${idx}" aria-label="Ir a foto ${idx + 1}">
              <img src="${resolveUrl(s.image)}" alt="${s.title}" />
            </button>
          `).join('');

          thumbsRow.querySelectorAll('.carousel-thumb-btn').forEach((tb) => {
            tb.addEventListener('click', () => {
              const i = parseInt(tb.getAttribute('data-index') || '0', 10);
              renderSlide(i);
            });
          });
        }

        renderSlide(0);
        modal.showModal();
      });
    });

    prevBtn?.addEventListener('click', () => renderSlide(currentIndex - 1));
    nextBtn?.addEventListener('click', () => renderSlide(currentIndex + 1));

    document.addEventListener('keydown', (e) => {
      if (modal?.open) {
        if (e.key === 'ArrowLeft') renderSlide(currentIndex - 1);
        if (e.key === 'ArrowRight') renderSlide(currentIndex + 1);
      }
    });

    closeBtn?.addEventListener('click', () => modal?.close());
    modal?.addEventListener('click', (e) => {
      if (e.target === modal) modal.close();
    });
  });
</script>
